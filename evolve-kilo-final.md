# Промпт для агента: создать скилл `evolve` для KiloCode (VS Code extension)

---

Ты — агент в KiloCode. Твоя задача — создать скилл `evolve`, который будет эволюционировать этот harness-проект после каждой значимой задачи.

**Главная идея**: пользователь завершает большую задачу → запускает `/evolve` → ты анализируешь сессию → предлагаешь конкретные улучшения harness → применяешь только то, что пользователь явно одобрил.

Действуй строго по шагам ниже. Не пропускай ни одного шага.

---

## ШАГ 1 — Аудит текущего harness

Прочитай следующие файлы (пропускай если не существуют, но фиксируй отсутствие):

```
AGENTS.md                          ← основная инструкция проекта
kilo.jsonc                         ← конфиг: skills.paths, instructions, etc.
.kilo/skills/*/SKILL.md            ← существующие project-level скиллы (только frontmatter!)
.kilo/rules/*.md                   ← project rules
.kilo/learnings/LEARNINGS.md       ← база знаний (если есть)
.kilo/learnings/ERRORS.md          ← база ошибок (если есть)
```

По результатам составь внутренний список:
- Какие скиллы уже есть (поле `name` из frontmatter)
- Какие rules загружены через `kilo.jsonc` → `instructions`
- Есть ли уже `.kilo/learnings/` с какими-либо файлами
- Какова текущая структура `.kilo/`

---

## ШАГ 2 — Создай структуру скилла

### 2.1 Создай `.kilo/skills/evolve/SKILL.md`

Содержимое файла:

```markdown
---
name: evolve
description: Post-task harness evolution. Analyzes the current session to extract corrections, patterns, and learnings, then proposes targeted updates to AGENTS.md, .kilo/rules/, and .kilo/skills/. Invoke after completing a significant task: /evolve, "evolve harness", "обнови harness", "что мы узнали".
---

# evolve — Harness Evolution Skill

## Когда использовать
- После завершения большой фичи, рефакторинга, сложной отладки
- Когда агент был поправлен пользователем 2+ раза за сессию
- Когда найден нетривиальный workaround или неочевидное решение
- По запросу: `/evolve`, "evolve harness", "обнови harness", "что мы узнали"

---

## ФАЗА 1 — Сбор сигналов

Просмотри всю историю текущего разговора. Ищи сигналы пяти типов:

**Тип A — Коррекции пользователя** [приоритет HIGH]
- "нет", "не так", "стоп", "не используй X", "всегда делай Y"
- Пользователь отменил действие агента и объяснил почему
- Одна и та же правка применена 2+ раз подряд

**Тип B — Нетривиальные решения** [приоритет HIGH]
- Решение потребовало 3+ итераций или смены подхода
- Найден workaround для проблемы инструмента/библиотеки/окружения
- Обнаружена неочевидная зависимость между компонентами

**Тип C — Паттерны проекта** [приоритет MEDIUM]
- Повторяющаяся последовательность действий (3+ раз в сессии)
- Соглашение проекта, которого нет в AGENTS.md
- Архитектурное решение с объяснённым "почему"

**Тип D — Провалы с причинами** [приоритет MEDIUM]
- Команда упала — зафиксировать команду, симптом, причину, fix
- Тест провалился неожиданным образом
- Инструмент повёл себя иначе чем ожидалось

**Тип E — Новые находки** [приоритет LOW]
- Более эффективный способ делать что-то стандартное
- Полезная команда/утилита для этого проекта
- Паттерн, достаточно самостоятельный чтобы стать отдельным скиллом

---

## ФАЗА 2 — Классификация и фильтрация

Для каждого найденного сигнала определи:

**Scope:**
- `global` — применимо в любом проекте → предложить добавить в `~/.kilo/rules/`
- `project` → в `.kilo/rules/[category].md` или в `AGENTS.md`
- `task` — разовое, не переиспользуемое → **отсеять**

**Тип изменения:**
- `rule` → правило в `.kilo/rules/[category].md` или новая секция в `AGENTS.md`
- `skill` → новый скилл в `.kilo/skills/[name]/SKILL.md`
- `learning` → запись в `.kilo/learnings/LEARNINGS.md`
- `error` → запись в `.kilo/learnings/ERRORS.md`

**Фильтры — отсеивай:**
- Разовая правка без явного паттерна
- Уже присутствует в существующих файлах (проверяй по аудиту из Шага 1)
- Проблема внешнего сервиса вне зоны контроля
- Слишком расплывчато ("будь аккуратнее", "пиши лучше")

**Правило промоции в AGENTS.md** — сигнал становится rule в AGENTS.md если:
- Встречается 3+ раз в сессии, ИЛИ
- Пользователь сказал "всегда", "никогда", "запомни", ИЛИ
- Касается критического поведения (git, файловая система, архитектура, API контракты)

---

## ФАЗА 3 — Показ предложений

Выведи структурированный список:

```
══════════════════════════════════════════════════
HARNESS EVOLUTION PROPOSAL  |  KiloCode
Задача сессии: [краткое описание]
Дата: [YYYY-MM-DD]
Сигналов найдено: [N]  |  Предложений: [M]
══════════════════════════════════════════════════

[RULE-1] HIGH  →  AGENTS.md :: добавить в секцию "[секция]"
  Правило: "[точная формулировка]"
  Источник: [описание сигнала]
  Pattern-Key: [kebab-case-ключ]

[RULE-2] MEDIUM  →  .kilo/rules/android.md :: секция "Kotlin"
  Правило: "[точная формулировка]"
  ...

[SKILL-1] HIGH  →  Создать .kilo/skills/[name]/SKILL.md
  Проблема: [что происходит]
  Решение: [что нашли]
  Переиспользуемость: HIGH / MEDIUM

[LEARNING-1] MEDIUM  →  .kilo/learnings/LEARNINGS.md
  Запись: "[содержание]"
  Scope: project

[ERROR-1] LOW  →  .kilo/learnings/ERRORS.md
  Команда: [команда]
  Симптом / Причина / Fix: [описание]

══════════════════════════════════════════════════
Применить? →  y  |  n  |  1,3,5  |  skip SKILL  |  edit 2
```

---

## ФАЗА 4 — Интерактивное применение

Жди ответа пользователя и действуй по таблице:

| Ввод | Действие |
|---|---|
| `y` | Применить все предложения |
| `n` | Ничего не менять, завершить |
| `1,3,5` | Применить только эти номера |
| `skip SKILL` | Применить всё кроме SKILL-предложений |
| `edit 2` | Показать предложение 2, принять правку, затем применить |

### Правила записи для каждого типа:

**RULE → AGENTS.md или .kilo/rules/[category].md**
1. Прочитай весь целевой файл
2. Найди подходящую секцию (или создай новую с заголовком)
3. Вставь правило кратко: 1-2 строки, конкретно и без расплывчатости
4. Покажи diff перед записью, жди подтверждения

**SKILL → .kilo/skills/[name]/SKILL.md**
1. Проверь по аудиту что скилла с таким `name` нет
2. Создай директорию `.kilo/skills/[name]/`
3. Создай `SKILL.md` по шаблону (см. ниже)
4. Имя директории = значение поля `name` в frontmatter (строчные буквы, цифры, дефисы)

**LEARNING → .kilo/learnings/LEARNINGS.md**
1. Если файла нет — создай (см. шаблон ниже)
2. Добавь новую запись в конец файла между маркерами
3. Никогда не удаляй и не редактируй существующие записи

**ERROR → .kilo/learnings/ERRORS.md**
1. Если файла нет — создай
2. Добавь запись в конец, никогда не удаляй существующие

---

## ФАЗА 5 — Итоговый отчёт

```
✅ HARNESS EVOLVED
  + [N] правил → AGENTS.md / .kilo/rules/
  + [N] скилл(ов) создан → .kilo/skills/
  + [N] записей → .kilo/learnings/LEARNINGS.md
  + [N] записей → .kilo/learnings/ERRORS.md

⚠️  Важно: начни новую сессию чтобы KiloCode подхватил новые скиллы.
    Новые rules вступят в силу после перезагрузки конфига.
```

---

## Шаблон нового скилла (.kilo/skills/[name]/SKILL.md)

```markdown
---
name: [skill-name]
description: [ОДНА строка до 1024 символов — что делает, когда применять, триггерные слова]
metadata:
  extracted-from-session: [YYYY-MM-DD]
  pattern-key: [kebab-case]
  recurrence-count: 1
---

# [Название]

## Проблема
[Что происходит, симптомы, когда возникает]

## Когда применять
- [Конкретная ситуация 1]
- [Конкретная ситуация 2]

## Решение

### Шаг 1
[Действие]

### Шаг 2
[Действие]

## Проверка
[Как убедиться что сработало]

## Ограничения
[Где этот подход не работает]
```

**Критические правила KiloCode для SKILL.md:**
- `name`: строчные буквы, цифры, дефисы. Максимум 64 символа. Должен точно совпадать с именем директории
- `description`: одна строка (никакого multiline YAML!), максимум 1024 символа
- `metadata` (опционально): произвольные key-value пары — используй для трекинга recurrence-count

---

## Шаблон записи в LEARNINGS.md

```markdown
## [YYYY-MM-DD] | [pattern-key]
**Тип**: correction | pattern | discovery | workaround
**Модуль**: [название компонента/инструмента]
**Приоритет**: HIGH | MEDIUM | LOW
**Recurrence-Count**: 1

### Суть
[1-2 предложения — конкретно и применимо]

### Когда важно
[Контекст: при каких условиях это знание нужно]

### Применение
[Конкретное действие агента]

---
```

## Шаблон записи в ERRORS.md

```markdown
## [YYYY-MM-DD] | [команда или компонент]
**Команда**: `[полная команда]`
**Симптом**: [что видит пользователь]
**Причина**: [почему происходит]
**Fix**: [что сработало]
**Статус**: resolved | recurring | workaround

---
```
```

---

## ШАГ 3 — Создай базовые файлы learnings (если не существуют)

### `.kilo/learnings/LEARNINGS.md`

```markdown
# Project Learnings

> База знаний проекта. Пополняется через `/evolve` после значимых задач.
> Правило: ТОЛЬКО ДОБАВЛЯТЬ записи, никогда не удалять и не редактировать существующие.

<!-- LEARNINGS:START -->
<!-- LEARNINGS:END -->
```

### `.kilo/learnings/ERRORS.md`

```markdown
# Known Errors & Fixes

> Документированные ошибки и их решения.
> Правило: ТОЛЬКО ДОБАВЛЯТЬ записи, никогда не удалять и не редактировать существующие.

<!-- ERRORS:START -->
<!-- ERRORS:END -->
```

---

## ШАГ 4 — Обнови kilo.jsonc

Прочитай `kilo.jsonc`. Если он не существует — создай. Добавь скилл `evolve` в конфиг так, чтобы он загружался автоматически:

```jsonc
{
  // Существующие настройки сохрани без изменений
  "skills": {
    "paths": [".kilo/skills"]
  }
}
```

Если `skills.paths` уже есть — убедись что `.kilo/skills` в списке. Не дублируй.

---

## ШАГ 5 — Добавь секцию в AGENTS.md

Прочитай `AGENTS.md`. Если секции "Harness Evolution" ещё нет — добавь в конец:

```markdown
## Harness Evolution

После завершения значимой задачи запускай `/evolve` для обновления harness.

### Накопленный опыт
- `.kilo/learnings/LEARNINGS.md` — паттерны, открытия, решения
- `.kilo/learnings/ERRORS.md` — известные ошибки и их fix'ы

### Правило использования
**До начала сложной задачи**: прочитай `.kilo/learnings/LEARNINGS.md` — там может быть уже готовое решение.
**После завершения значимой задачи**: запусти `/evolve`.
```

Примечание: `AGENTS.md` — write-protected файл в KiloCode, пользователь увидит запрос на подтверждение изменения. Это нормально.

---

## ШАГ 6 — Валидация

Проверь перед финишем:

1. `.kilo/skills/evolve/SKILL.md` существует, frontmatter валиден: `name: evolve`, `description` — одна строка
2. `kilo.jsonc` содержит `.kilo/skills` в `skills.paths`
3. `.kilo/learnings/LEARNINGS.md` и `ERRORS.md` созданы
4. В `AGENTS.md` добавлена секция Harness Evolution

Покажи итоговую структуру:

```
.kilo/
  skills/
    evolve/
      SKILL.md         ✅
  learnings/
    LEARNINGS.md       ✅
    ERRORS.md          ✅
kilo.jsonc             ✅ (skills.paths обновлён)
AGENTS.md              ✅ (секция Harness Evolution добавлена)
```

---

## Жёсткие ограничения при реализации

- **НЕ** изменяй `AGENTS.md` без показа diff и явного `y` от пользователя
- **НЕ** удаляй существующие записи из `LEARNINGS.md` или `ERRORS.md` — только добавляй
- **НЕ** перезаписывай существующий скилл без явного подтверждения — предложи обновить
- **НЕ** используй multiline YAML для `description` в SKILL.md
- **НЕ** ставь `name` в frontmatter отличный от имени директории — скилл будет проигнорирован
- Скиллы подхватываются только при старте новой сессии — предупреди пользователя

---

## Опциональные расширения (предложи после создания базовой версии)

> Базовая версия готова. Хочешь добавить расширения?
>
> **A)** Счётчик повторений: при `recurrence-count` ≥ 3 в LEARNINGS.md — автоматически предлагать промоцию в AGENTS.md при следующем `/evolve`
>
> **B)** `/evolve --dry-run` — только показ предложений, без записи файлов (для ревью)
>
> **C)** git-интеграция: после применения предложить коммит с сообщением `harness: [краткое описание изменений]`
>
> **D)** Глобальный уровень: сигналы с scope=global копировать в `~/.kilo/rules/` для переиспользования в других проектах
