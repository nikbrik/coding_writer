---
name: scope-lock
description: Compatibility adapter for the shared scope-lock protocol; loads the canonical implementation from .agents/skills/scope-lock.
---

# scope-lock - Kilo adapter

Load and follow `.agents/skills/scope-lock/SKILL.md`.

The canonical protocol, templates, roles, guard rules, handoff rules, and validation rules live under `.agents/skills/scope-lock/`.

This adapter must stay thin. Do not duplicate or fork shared scope policy here.
